"""Shared backend constants."""

# Default SQLite database path — used by both app.py and backend/scripts/manage_users.py.
# Override via SQLITE_DB_PATH environment variable.
DEFAULT_DB_PATH = '/var/lib/flxcontabilidad/users.db'
